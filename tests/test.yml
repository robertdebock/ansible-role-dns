---
- hosts: localhost
  remote_user: root

  roles:
    - dns

  tasks:
    - name: configure system to resolve to localhost
      command: echo "nameserver 127.0.0.1" > /etc/resolv.conf

    - name: lookup www.example.com
      command: getent www.example.com
