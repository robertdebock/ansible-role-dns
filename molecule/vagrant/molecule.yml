---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint:
  name: yamllint
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: dns-debian
    box: debian/stretch64
  - name: dns-fedora
    box: fedora/29-cloud-base
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: vagrant
